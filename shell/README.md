### Shell Shortcuts (`shell/dev_aliases.sh`)
Includes productivity aliases for:
- Navigation (e.g., `desk`, `docs`, `tmp`)
- Git shortcuts (`gs`, `ga`, `gp`, etc.)
- Python (`py`, `serve`, `venv-on`)
- File search & extract helpers
- Process and port monitoring

To use:
```bash
source shell/dev_aliases.sh
